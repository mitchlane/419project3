#include <iostream>
#include "minicsv.h"
#include "DataItems.hpp"

class ParseAndWrite
{
  public:
    ParseAndWrite(route* r, airport* ap);
    void parse();
    void write(result res);
  
  private:
};
